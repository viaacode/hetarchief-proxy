query getFileStoredAtByRepresentationId($representationId: String) {
	graph_includes(
		where: {
			representation_id: { _eq: $representationId }
			file: { ebucore_has_mime_type: { _in: ["video/mp4", "audio/mp4"] } }
		}
		limit: 1
	) {
		file {
			premis_stored_at
		}
	}
}
