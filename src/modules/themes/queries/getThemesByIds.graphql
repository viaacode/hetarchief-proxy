query getThemesByIds($ids: [uuid!]!) {
	app_theme(where: { id: { _in: $ids } }) {
		id
		slug
		name_nl
		name_en
		description_nl
		description_en
		image_url
		content_page_path_nl
		content_page_path_en
		updated_at
	}
}
