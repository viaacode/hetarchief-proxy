query getUnreadMessageOverviewForProfile($profileId: uuid!) {
	app_material_request_unread_message_overview(
		where: { receiver_profile_id: { _eq: $profileId } }
	) {
		material_request_id
		is_outgoing
	}
}
