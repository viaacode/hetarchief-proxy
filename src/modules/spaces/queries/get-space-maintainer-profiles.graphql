query getSpaceMaintainerProfiles($spaceId: uuid) {
	maintainer_users_profile(
		where: {
			maintainer: { visitor_space: { id: { _eq: $spaceId } } }
			profile: { group: { name: { _eq: "CP_ADMIN" } } }
		}
	) {
		users_profile_id
		profile {
			mail
		}
	}
}
