query findMaintainerMail($materialRequestId: uuid!) {
	app_material_requests(where: { id: { _eq: $materialRequestId } }) {
		id
		object {
			maintainer {
				schema_identifier
				information {
					contact_point
				}
			}
		}
	}
}
