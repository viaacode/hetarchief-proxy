query findAllNavigationItems {
	app_navigation(order_by: { placement: asc, position: asc }) {
		content_path
		content_type
		link_target
		placement
		position
		id
		icon_name
		user_group_ids
		label
		updated_at
		description
		created_at
		content_id
		tooltip
	}
}
