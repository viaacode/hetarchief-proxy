query findNavigation {
	app_navigation(distinct_on: placement, order_by: { placement: asc }) {
		id
		description
		placement
		tooltip
		user_group_ids
	}
	app_navigation_aggregate(distinct_on: placement) {
		aggregate {
			count
		}
	}
}
