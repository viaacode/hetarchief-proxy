query findNavigationById($id: uuid!) {
	cms_navigation_element(where: { id: { _eq: $id } }) {
		id
		created_at
		description
		user_group_ids
		icon_name
		label
		link_target
		placement
		position
		updated_at
		content_type
		content_path
		tooltip
	}
}
